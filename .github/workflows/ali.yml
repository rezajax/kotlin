name : AliBuild

on: [push, workflow_dispatch]

jobs:
  aliBuild:
    runs-on: ubuntu-latest
    steps:
      - name: Install and Run Cowsay
      - run: |
            sudo apt-get install cowsay
            cowsay "Hello World"


#  rezaPublishOnGhPages:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout sources
#        uses: actions/checkout@v4
#
#      - name: "Publishing to GitHub Pages"